
import ffmpeg

def merge_audio_video(video_path, audio_path, output_path):
    print("🔊 Merging audio with video using ffmpeg...")
    ffmpeg.input(video_path).output(audio_path, shortest=None)
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    ffmpeg.input(video_path).output(audio_path, shortest=None)
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )
    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    (
        ffmpeg
        .input(video_path)
        .output(audio_path, shortest=None)
    )

    print(f"🎬 Video with audio saved: {output_path}")
